import PropTypes from 'prop-types';

const Blog = ({props}) => {
    // console.log(props);
    return (
        <div>
            
        </div>
    );
};
Blog.propTypes={
    props:PropTypes.object.isRequired
}
export default Blog;